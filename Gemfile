source "https://rubygems.org"

# 核心依赖
gem "github-pages", "~> 228", group: :jekyll_plugins  # 匹配 GitHub Pages 最新版本
gem "bundler", "~> 2.2.33"  # 指定兼容版本

# 开发工具
gem "jekyll-compose", "~> 0.12", group: [:jekyll_plugins]  # 写作辅助
gem "jekyll-remote-theme", "~> 0.4"  # 远程主题支持

# 可选保留 gemspec（如果项目需要打包）
gemspec
